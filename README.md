# To-doList

This project is a simple To-Do List web application that allows users to add, view, and delete tasks. The frontend is built with HTML, CSS/SCSS, and JavaScript, while the backend uses Node.js and Express to provide a REST API.
